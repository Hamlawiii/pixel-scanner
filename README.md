# pixel-scanner
A Node.js + Playwright tool that loads a webpage in headless Chromium and detects every ad and tracking pixel running on it — Meta, Google, TikTok, LinkedIn, and more — by inspecting network requests, cookies, and page scripts. Outputs a clear JSON report showing exactly what's tracking your visitors.
